apiVersion: v1
kind: Service

metadata:
  name: spring-app-service

spec:
  selector:
    app: spring-app
    color: blue

  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30082

  type: NodePort
